#ifndef _ACM_LAB_UTILS_H
#define _ACM_LAB_UTILS_H

#include <cstdint>

// Greatest Common Divisor functions
int GCD (int q, int r); // GCD of two integer numbers
int GCD (const int16_t* values, int count); // GCD of int16_ts in an array

#endif
